import React from "react";
import { Button, Box } from "@chakra-ui/react";

export const LogoutButton: React.FC<any> = () => {
  return (
    <Box>
      <Button>
        Logout
      </Button>
    </Box>
  );
};