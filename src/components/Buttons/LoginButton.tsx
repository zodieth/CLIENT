import React from "react";
import { Button, Box } from "@chakra-ui/react";

export const LoginButton: React.FC<any> = () => {
  return (
    <Box>
      <Button>
        Login
      </Button>
    </Box>
  );
};